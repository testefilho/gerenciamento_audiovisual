{
  "name": "evento-producao",
  "version": "0.1.0",
  "private": true,
  "scripts": {
  "serve": "http-server -c-1 . -p 5500",
    "smoke-test": "node scripts/smoke-test.js",
    "test:emulator": "npx firebase emulators:exec \"node tests/emulator-rules-test.js\"",
  "test": "npm run test:emulator",
  "start:local": "pwsh -NoProfile -File ./scripts/start-local.ps1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.1.0",
    "firebase-admin": "^13.4.0",
  "http-server": "^14.1.1",
  "firebase-tools": "^14.14.0",
  "husky": "^8.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
